# tests/

> L2 | 父级: ../CLAUDE.md

成员清单

deployment.test.mjs: 解析 Docker Compose JSON 并检查镜像 digest、HTTP Console、GitHub 插件制品、GHCR/npm 测试版发布、Harness 锁定 manifest、单一 application.yml、无外部 SQL 挂载/发布文件、无日志卷的只读 Server 与插件卷权限初始化、环境参数、幂等 bootstrap、默认免签名密钥与保留已有 key 的运维边界。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
