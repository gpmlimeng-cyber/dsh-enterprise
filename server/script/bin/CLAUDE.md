# bin/

> L2 | 父级: ../CLAUDE.md

成员清单

owndsh.sh: POSIX JAR 启停入口，start/restart 前台执行 Java 并保留 stdout/stderr，后台生命周期与日志采集交给进程管理器。
owndsh.bat: Windows JAR 启停入口，使用独立 Java 控制台窗口保留日志，以 JPS/taskkill 查询和停止进程。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
