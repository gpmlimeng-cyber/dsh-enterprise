{
  "name": "@owndsh/llm-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "exports": {
    ".": {
      "types": "./lib/index.d.ts",
      "default": "./lib/index.js"
    }
  },
  "files": ["lib", "README.md"],
  "scripts": {
    "build": "pnpm --filter @owndsh/llm-gateway^... run build && tsc -p tsconfig.json",
    "typecheck": "pnpm --filter @owndsh/llm-gateway^... run build && tsc -p tsconfig.json --noEmit",
    "test": "vitest run tests"
  },
  "dependencies": {
    "@owndsh/platform-client": "workspace:*"
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "^4.0.1",
    "@deepseek-ai/dsh-llm-pi-ai": "^0.1.5-rc.2"
  },
  "devDependencies": {
    "@deepseek-ai/dsh-llm-pi-ai": "0.1.5-rc.2"
  }
}
